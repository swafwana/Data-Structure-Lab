#include <stdio.h>
#int main()
{
	int array[10],i,sum=0;
	for (i=0;i<10;i++)
	printf("Enter elements of array");
	{
		scanf("%d",array[10]);
	}

	for(i=0;i<10;i++)
	{
		sum=sum+array[i]
	}
	printf("%d",&sum);
	return(0);

